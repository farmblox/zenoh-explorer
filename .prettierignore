dist
target
node_modules
pnpm-lock.yaml
src/ipc/generated
src-tauri/gen
design/mockup

# Design-canvas working files and the page they seed. The seeded page is a
# ~2 MiB generated payload, and the .dc.html sources are authored for another
# tool's parser — reformatting either only risks breaking them.
design/canvas

# Generated by each plugin's build.rs on every build. Prettier reformatting
# them just means the next `cargo build` reformats them back.
crates/*/permissions/schemas
crates/*/permissions/autogenerated
